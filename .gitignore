# Per-creator config — never commit Supabase URLs/keys
supabase/config.sh

# OS / editor noise
.DS_Store
*.swp
.vscode/
.idea/

# Local test artifacts
*.jsonl
.cursor-*
.start-*
supabase/.temp/

# Per-skill Supabase configs (each fork has its own; never commit)
**/supabase/config.sh
